<div></div>
{% schema %}
{
  "settings": [
    {
      "type": "select",
      "id": "timer_on_end",
      "label": "When the countdown reaches zero",
      "options": [
        {
          "value": "hide",
          "label": "Hide the countdown, keep the offer"
        },
        {
          "value": "restart",
          "label": "Start it again"
        }
      ],
      "info": "Starting it again is the pattern the guidelines above call out.",
      "default": "hide"
    }
  ],
  "name": "zz b"
}
{% endschema %}
