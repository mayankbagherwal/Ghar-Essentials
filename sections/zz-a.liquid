<div></div>
{% schema %}
{
  "settings": [
    {
      "type": "range",
      "id": "timer_minutes",
      "min": 1,
      "max": 60,
      "step": 1,
      "unit": "min",
      "label": "Minutes to count down per visit",
      "info": "Only used when no end date is set above.",
      "default": 5
    }
  ],
  "name": "zz a"
}
{% endschema %}
